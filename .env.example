# 环境变量示例文件

# API 基础 URL（生产环境）
# 部署到 Railway 后，替换为实际的 Railway 后端 URL
PROJECT_DOMAIN=https://your-backend-url.up.railway.app

# 微信小程序 AppID（可选，如需发布微信小程序）
TARO_APP_WEAPP_APPID=

# 输出目录配置（通常不需要修改）
OUTPUT_ROOT=dist-web

# Coze 项目域名默认值（可选）
COZE_PROJECT_DOMAIN_DEFAULT=
